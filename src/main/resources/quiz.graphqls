type Query {
    questions(page: Int, count: Int): [Question]
    users(page: Int, count: Int): [User]
}

type User {
    id: Int
    firstName: String
    lastName: String
    questions: [Question]
}

type Question {
    id: Int
    question: String
    user: User
}