# The user for which a question is asked
type User {
    id: Int
    firstName: String
    lastName: String
    questions: [Question]
}

# The question, they all relate to a user
type Question {
    id: Int
    question: String
    user: User
}